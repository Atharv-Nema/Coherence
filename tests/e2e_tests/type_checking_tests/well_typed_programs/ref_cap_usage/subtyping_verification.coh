actor Main {
    new create() {
        var a_iso: int iso = new iso[1] int(0);
        var a_tag: int tag = new tag[1] int(0);
        var a_val: int val = new val[1] int(0);
        var a_ref: int ref = new ref[1] int(0);
        var a_locked: int locked<A> = new locked<A>[1] int(0);
        a_tag = a_iso;
        a_tag = a_val;
        a_tag = a_ref;
        a_tag = a_locked;
        a_iso = (a_iso = nullptr);
        a_val = (a_iso = nullptr);
        a_ref = (a_iso = nullptr);
        a_locked = (a_iso = nullptr);
    }
}