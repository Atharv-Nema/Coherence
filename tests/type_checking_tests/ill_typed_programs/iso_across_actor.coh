actor Other {
    new create() {}
    be iso_behaviour(int iso x) {}
}

actor Main {
    new create() {
        var other_instance: Other = new Other.create();
        var x: int iso = new[1] int(0);
        // sending a ref is not allowed
        other_instance->iso_behaviour(x);
    }
}